= form_for @photo, html: {multipart: true}, url: album_photos_path do |f|
  = f.label "Photo"
  = f.file_field :image, as: :file
  = f.label "Tag Line"
  = f.text_field :tag_line
  = f.submit "upload photo"
